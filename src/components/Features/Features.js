import React from "react";
import useStyles from "./style";
const Features = () => {
  const classes = useStyles();
  return <section className={classes.features}>Features</section>;
};

export default Features;
